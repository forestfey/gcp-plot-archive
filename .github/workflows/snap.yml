name: GCP plot daily snapshot
on:
  schedule:
    - cron: '0 16 * * *'   # catches CEST
    - cron: '0 17 * * *'   # catches CET

jobs:
  grab:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Skip if today’s file already exists
        run: |
          file="plots/gcp24h_$(date -u +%Y-%m-%d)_18-00.png"
          if [ -f "$file" ]; then
            echo "Snapshot already taken." && exit 0
          fi
          curl -s -o "$file" https://global-mind.org/gcpdot/plot.png
      - name: Commit & push
        run: |
          git config user.name  "gcp-bot"
          git config user.email "bot@example.com"
          git add plots/*.png
          git commit -m "auto snapshot $(date -u)" || echo "Nothing to commit"
          git push
